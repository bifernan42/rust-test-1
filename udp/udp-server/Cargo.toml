[package]
name = "udp-server"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = { version = "0.4.38", features = ["serde"] }
serde = {version = "1.0", features = ["derive"]}
bincode = "1.3"
